{
  "name": "@tuomo/root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^8.2.4"
  },
  "version": "0.0.0"
}
